// Const, constante, no puede ser reasignado,
// tiene el mismo valor inicial hasta el final
const producto = 'Tablet';

//producto = "Monitor";
console.log(producto);

// Los const siempre se deben inicializar con un valor,
// no se puede asignar un valor después
const precio = 20;
console.log(precio);